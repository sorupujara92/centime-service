package com.centime.repository;

import com.centime.model.ParentIdColor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParentIdColorRepository extends JpaRepository<ParentIdColor, Long> {
}
